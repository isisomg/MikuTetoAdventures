using System;
using UnityEngine;

public class TetoVariables : MonoBehaviour
{

    public Boolean isAlive = true;        // check to see if teto is alive or not, can be changed by other scripts.
    public Boolean winCondition = false;    // check to see if teto has reached her win condition, can be changed by other scripts.


    //void Start()
    //{

    //}

    //// Update is called once per frame
    //void Update()
    //{

    //}
}
