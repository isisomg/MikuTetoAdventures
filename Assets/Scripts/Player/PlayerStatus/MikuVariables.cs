using System;
using UnityEngine;

public class MikuVariables : MonoBehaviour
{
    public Boolean isAlive = true;        // check to see if miku is alive or not, can be changed by other scripts.
    public Boolean winCondition = false;    // check to see if miku has reached her win condition, can be changed by other scripts.
}
